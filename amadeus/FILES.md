# 📁 Amadeus 插件系统 - 文件清单

## 项目结构概览

```
amadeus/
├── 📄 Cargo.toml                        # 项目配置和依赖
├── 📄 Cargo.lock                        # 依赖锁定文件
│
├── 📖 README.md                         # 项目主文档
├── 📖 QUICKSTART.md                     # 快速上手指南（5分钟入门）
├── 📖 PLUGIN_SYSTEM.md                  # 完整设计文档
├── 📖 ADVANCED_FEATURES.md              # 高级功能指南
├── 📖 ARCHITECTURE.md                   # 系统架构图解
├── 📖 FILES.md                          # 本文件 - 文件清单
│
├── 📋 plugins_config.example.json       # JSON 配置文件示例
│
├── 📂 src/                              # 源代码目录
│   ├── 🦀 main.rs                      # 程序入口点
│   ├── 🦀 plugin.rs                    # 插件系统核心
│   │   ├── Plugin trait                # 插件接口定义
│   │   ├── PluginMetadata             # 插件元数据结构
│   │   └── PluginRegistry             # 插件注册表
│   │
│   └── 📂 plugins/                     # 插件目录
│       ├── 🦀 mod.rs                   # 插件模块导出
│       │
│       ├── 🦀 code4rena.rs            # Code4rena 插件（单文件）
│       │   ├── Code4renaPlugin        # 插件结构体
│       │   └── Plugin trait 实现       # 生命周期方法
│       │
│       └── 📂 example_plugin/         # Example 插件（多文件）
│           ├── 🦀 mod.rs              # 插件主模块
│           │   ├── ExamplePlugin      # 插件结构体
│           │   └── Plugin 实现         # 主要逻辑
│           │
│           ├── 🦀 config.rs           # 配置管理模块
│           │   ├── PluginConfig       # 配置结构
│           │   └── load/validate      # 配置方法
│           │
│           └── 🦀 handler.rs          # 数据处理模块
│               ├── DataHandler        # 处理器结构
│               └── process/cleanup    # 处理方法
│
└── 📂 target/                          # 编译输出（自动生成）
    ├── debug/                          # 调试版本
    └── release/                        # 发布版本
```

## 文件说明

### 📖 文档文件

| 文件名 | 用途 | 适合人群 |
|--------|------|----------|
| `README.md` | 项目概览、特性介绍 | 所有人 |
| `QUICKSTART.md` | 5分钟快速入门教程 | 新手开发者 |
| `PLUGIN_SYSTEM.md` | 完整设计文档、使用指南 | 插件开发者 |
| `ADVANCED_FEATURES.md` | 配置、异步、通信等高级功能 | 高级开发者 |
| `ARCHITECTURE.md` | 系统架构、数据流、设计模式 | 架构师、维护者 |
| `FILES.md` | 文件清单和说明 | 新加入者 |

### 🦀 核心代码文件

#### `src/main.rs` - 程序入口
```rust
// 功能：
- 创建 PluginRegistry
- 注册所有插件
- 执行插件生命周期
- 导出元数据
- 错误处理

// 行数：约 41 行
```

#### `src/plugin.rs` - 插件系统核心
```rust
// 包含：
- Plugin trait（插件接口）
- PluginMetadata（元数据结构）
- PluginRegistry（注册表）

// 功能：
- 定义插件接口
- 管理插件生命周期
- JSON 配置加载/保存
- 插件列表和导出

// 行数：约 197 行
```

### 🔌 插件文件

#### `src/plugins/mod.rs` - 插件模块导出
```rust
// 功能：
- 导出所有插件模块
- 方便 main.rs 引用

// 行数：约 5 行
```

#### `src/plugins/code4rena.rs` - 单文件插件示例
```rust
// 结构：
- Code4renaPlugin struct
- Plugin trait 实现
- 安全扫描相关逻辑

// 特点：
- 简单插件结构
- 适合独立功能
- 易于理解

// 行数：约 80 行
```

#### `src/plugins/example_plugin/` - 多文件插件示例

**mod.rs** - 主模块
```rust
// 功能：
- 定义插件主结构
- 协调各子模块
- 实现 Plugin trait

// 行数：约 90 行
```

**config.rs** - 配置模块
```rust
// 功能：
- 定义配置结构
- 加载配置
- 验证配置

// 行数：约 45 行
```

**handler.rs** - 处理器模块
```rust
// 功能：
- 数据处理逻辑
- 资源管理
- 统计信息

// 行数：约 50 行
```

### 📋 配置文件

#### `plugins_config.example.json` - 配置示例
```json
// 包含：
- 插件元数据定义
- 配置参数示例
- 自定义属性示例

// 用途：
- 作为配置模板
- 展示配置格式
- 开发参考
```

#### `Cargo.toml` - 项目配置
```toml
// 包含：
- 项目信息
- 依赖声明
- 编译选项

// 依赖：
- anyhow: 错误处理
- serde: 序列化
- serde_json: JSON 支持
- iceoryx2: 进程通信（可选）
```

## 文件统计

### 代码统计
```
语言：Rust
核心代码：约 500 行
文档：约 1500+ 行
总计：约 2000+ 行

代码分布：
- src/plugin.rs:        197 行 (39%)
- src/plugins/:         265 行 (53%)
- src/main.rs:           41 行 (8%)
```

### 文档统计
```
- README.md:              约 250 行
- QUICKSTART.md:          约 80 行
- PLUGIN_SYSTEM.md:       约 650 行
- ADVANCED_FEATURES.md:   约 450 行
- ARCHITECTURE.md:        约 550 行
- FILES.md:               本文件
```

## 如何使用这些文件

### 🚀 快速开始
1. 阅读 `README.md` - 了解项目概况
2. 运行 `cargo run` - 看看效果
3. 阅读 `QUICKSTART.md` - 创建第一个插件

### 📚 深入学习
1. 阅读 `PLUGIN_SYSTEM.md` - 理解设计理念
2. 查看 `src/plugins/code4rena.rs` - 学习单文件插件
3. 查看 `src/plugins/example_plugin/` - 学习多文件插件

### 🔧 高级开发
1. 阅读 `ADVANCED_FEATURES.md` - 学习高级功能
2. 阅读 `ARCHITECTURE.md` - 理解系统架构
3. 查看 `plugins_config.example.json` - 配置示例

### 🏗️ 架构理解
1. 阅读 `ARCHITECTURE.md` - 系统架构
2. 查看 `src/plugin.rs` - 核心实现
3. 理解数据流和设计模式

## 添加新文件

### 添加新的单文件插件
```bash
# 1. 创建插件文件
touch src/plugins/my_plugin.rs

# 2. 编辑 src/plugins/mod.rs
echo "pub mod my_plugin;" >> src/plugins/mod.rs

# 3. 在 main.rs 中注册
# use plugins::my_plugin::MyPlugin;
# registry.register(MyPlugin::new());
```

### 添加新的多文件插件
```bash
# 1. 创建插件目录
mkdir -p src/plugins/my_plugin

# 2. 创建文件
touch src/plugins/my_plugin/mod.rs
touch src/plugins/my_plugin/config.rs
touch src/plugins/my_plugin/handler.rs

# 3. 导出模块
echo "pub mod my_plugin;" >> src/plugins/mod.rs
```

## 文件依赖关系

```
main.rs
  ├─► plugin.rs
  │     ├─► Plugin trait
  │     ├─► PluginMetadata
  │     └─► PluginRegistry
  │
  └─► plugins/
        ├─► code4rena.rs
        │     └─► Plugin trait
        │
        └─► example_plugin/
              ├─► mod.rs (实现 Plugin trait)
              ├─► config.rs
              └─► handler.rs
```

## 编译产物

### Debug 构建
```bash
cargo build
# 输出: target/debug/amadeus
# 大小: 约 10-20 MB（包含调试信息）
```

### Release 构建
```bash
cargo build --release
# 输出: target/release/amadeus
# 大小: 约 2-5 MB（优化后）
```

## 文件权限

所有源代码文件建议权限：
- 源文件：`644` (rw-r--r--)
- 脚本文件：`755` (rwxr-xr-x)
- 文档文件：`644` (rw-r--r--)

## 版本控制

### Git 忽略文件
```gitignore
# 编译产物
/target/
**/*.rs.bk
*.pdb

# 配置文件（不包含示例）
plugins_config.json

# IDE 文件
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# 日志文件
*.log
```

---

这份文件清单帮助您快速了解项目结构！🗂️

